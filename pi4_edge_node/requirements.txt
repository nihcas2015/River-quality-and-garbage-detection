paho-mqtt==1.6.1
ultralytics>=8.0.0
numpy>=1.24.0
requests>=2.31.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0
# onnxruntime is optional — OpenCV DNN backend is used by default
# Camera capture uses the libcamera-still CLI tool (pre-installed on
# Raspberry Pi OS Bookworm). No picamera2 Python dependency is needed.
# On Pi4, OpenCV DNN reads best.onnx directly — avoids 'Illegal instruction'
# crashes from PyTorch/onnxruntime on ARM Cortex-A72.
