paho-mqtt>=1.6.1
ultralytics>=8.0.0
numpy>=1.24.0
requests>=2.31.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# HiveMQ cloud MQTT uses TLS — ssl is built into Python, no extra install needed

# Camera (install ONE):
# pip install picamera2              ← recommended for Pi4 Bookworm
# sudo apt install -y libcamera-tools ← fallback

# Model (place ONE in project directory):
# best.onnx  ← faster on Pi4 (convert: yolo export model=best.pt format=onnx)
# best.pt    ← original PyTorch weights
